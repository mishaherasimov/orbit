description: >
  This job is using ruby

executor: ruby

parameters:
  num_one:
    type: string
    default: "0"
  num_two:
    type: string
    default: "0"

steps:
  - run:
      name: Testing ruby job
      shell: ruby
      environment:
        NUMBER_ONE: << parameters.num_one >>
        NUMBER_TWO: << parameters.num_two >>
      command: <<include(scripts/hello.rb)>>
